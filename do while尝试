#include <stdio.h>
int main()
{
	int n = 0, k = 0;
	do {
		if (n % 3 == 0 || n % 5 == 0)
		{
			k++;
			printf("%d ", n);
		}
		if (k % 5 == 0)
		{
			printf("\n");
		}
		n++;
	} while (n<101);
	return 0;
}
