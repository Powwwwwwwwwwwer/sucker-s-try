#include <stdio.h>
main()
{
	int a = 0, b = 0, c = 0, d;
	do
	{
		a++;
		if (a == 10)
		{
			a = 0;
			b++;
		}
		if (b == 10)
		{
			b = 0;
			c++;
		}
		d = a + b * 10 + c * 100;
		if (a * a * a + b * b * b + c * c * c == d)
		printf("%d ", d); 
	} while (d<1000);
		return 0;
}
